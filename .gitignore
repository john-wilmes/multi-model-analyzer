node_modules/
dist/
!apps/dashboard/dist/
*.tsbuildinfo
.env
.env.local

# Storage data (uncompressed DB ignored, compressed .gz tracked for distribution)
data/*.db
data/*.db-journal
data/*.db-wal
!data/*.db.gz

# Config with local paths
mma.config*.json

# OS files
.DS_Store
Thumbs.db

# IDE / tools
.vscode/
.idea/
.serena/
.claude/settings.local.json
*.swp
*.swo

# WASM grammars — vendored in git (3.3MB total, avoids build-time dependency)
# Refreshed by: npm run build:wasm -w packages/parsing

# Test coverage
coverage/

# Corpus data (local only)
data-*/

# Test results
test-results/
